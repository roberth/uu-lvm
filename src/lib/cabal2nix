#!/bin/sh
# Run this when the cabal dependencies are updated

( echo "# This file is autogenerated by the ./cabal2nix script."
  echo "# Re-run ./cabal2nix when the dependencies in the cabal"
  echo "# file have changed."
  echo
  nix-shell -p cabal2nix --run 'cabal2nix .'
) >lvmlib.nix
